# bird_app v2 인계 문서

작성일 2026-09-21 · 출처: v1 저장소(`../bird_app v1/`)에서 나눈 설계 대화

이 문서는 **v2 계획서를 쓰는 세션에 넘기는 입력**이다. v2 폴더에서 새로 여는 세션은 v1의
CLAUDE.md·메모리·대화 내용을 전혀 모른다. 계획에 필요한 결정과 근거를 여기에 모았다.

표기 규칙:

- **[확정]** 사용자가 직접 정한 것. 다시 묻지 말 것.
- **[권장]** 대화에서 제안됐고 사용자가 반대하지 않았지만, 명시적으로 확정하지는 않은 것. 계획서에서 확정하거나 대안을 제시한다.
- **[미정]** 계획서가 답해야 하는 것.
- 요금·한도·라이선스 수치는 **대화 시점의 기억값**이다. 계획서에 옮기기 전에 공식 문서로 재확인한다.

---

## 1. 목표

- **[확정]** v1(로컬 전용 테스트 앱)을 그대로 두고, **Vercel에 올려 남도 쓸 수 있는 v2를 새로 만든다.** 디자인부터 완전히 새로 한다.
- **[확정]** **무료·비상업 배포.** 돈을 받지 않는다. 운영 비용은 사실상 0원이 목표다.
- **[확정]** 기능 범위는 세 가지다.
  1. 탐조 기록 정리 (사진·EXIF·위치·지도·꾸미기 관찰지)
  2. 사진에서 새 탐지(object detection) → 크롭 → LLM에 넘겨 종 판정
  3. 새소리를 분석해 어떤 새인지 판정
- 배경: 사용자는 **현장 사용**을 중요하게 본다 (2026-09-16). 카메라 사진을 폰으로 옮겨 바로 종을 찾고, 밖에서 새소리를 빠르게 판별하려 한다. Lightroom 보정 후 PC에서 넣는 흐름도 계속 쓴다. 그래서 **폰 브라우저에서 돌아가는 것이 1급 요구**다. 플레이스토어·토스 인앱(앱인토스)은 "나중에 해보고 싶다" 수준이며 v2 범위가 아니다.

## 2. 아키텍처 결정

```
┌─────────────── 사용자 브라우저 (폰/PC) ───────────────┐
│  React 앱 (Vercel 정적 배포)                           │
│  ├ 기록·사진·초안: 브라우저 로컬 저장소                 │
│  ├ 새 탐지 모델, 새소리 모델: 브라우저 안에서 추론       │
│  └ 저장 파일(ZIP) 다운로드 / 업로드로 이어 쓰기          │
└───────────────┬───────────────────────────────────────┘
                │ 크롭 사진만 (위치 EXIF 없음)
                ▼
        Vercel 함수 (얇은 프록시: 토큰·한도·SSRF 방어)
                │
      ┌─────────┴──────────┐
      ▼                    ▼
 사용자 자기 API 키      터널 → 운영자 PC의 로컬 LLM
 (있으면 이쪽)           (키 없을 때 폴백)
```

- **[확정] 사진과 기록은 서버에 저장하지 않는다.** 저장 비용을 없애고 개인정보 부담을 지지 않기 위해서다.
- **[확정] 이어 쓰기는 파일로 한다.** 작업 중간에 파일을 내려받아 PC에 보관하고, 나중에 업로드해서 이어서 작성한다. 가능함을 확인했다 (브라우저만으로 된다, 서버 불필요).
- **[권장] 브라우저 로컬 저장소(IndexedDB/OPFS) 자동 저장을 병행한다.** 파일만 쓰면 다운로드 없이 탭을 닫는 순간 작업이 사라지고, 사진이 수백 장이면 매번 수백 MB ZIP을 받아야 한다. 평소엔 자동 저장, 파일은 백업·기기 이동용.
- **[권장] 로그인 없음.** 서버에 사용자 데이터가 없으니 계정이 필요 없다. 부작용은 5절의 로컬 LLM 남용 방지 항목 참고.
- **[확정] LLM은 두 갈래.** 사용자가 자기 API 키를 넣으면 그것을 쓰고, 없으면 운영자(사용자 본인) PC의 로컬 LLM을 쓴다. 로컬 LLM은 OpenAI 호환 API로 서빙한다 (Unsloth로 준비한 모델. 실제 서빙 프로그램은 vLLM/llama.cpp/Ollama 류일 것 — 계획 때 확인).
- **[확정] 탐지·새소리 모델은 사용자 기기에서 돌린다.** 사진을 서버에 올리지 않으므로 서버 추론이 성립하지 않는다. 따라서 모델이 가벼워야 한다.
- **[권장] Vercel에는 정적 파일 + LLM 프록시 함수만 둔다.** 브라우저가 운영자 PC를 직접 부르면 주소와 토큰이 프론트 코드에 노출된다. 반드시 함수를 거친다. PC는 포트포워딩이 아니라 터널(Cloudflare Tunnel / Tailscale Funnel)로 연다.
- **[권장] 모델 파일·대용량 정적 자산은 Cloudflare R2(전송 무료)에 둔다.** 30~50MB 모델을 Vercel에서 서빙하면 Hobby 전송 한도를 깎는다.
- 나중 옵션 (v2 범위 밖, 구조만 막지 말 것): 사용자 Google Drive에 백업 ZIP 자동 저장, 유료 클라우드 동기화, PC 크롬 한정 File System Access API로 "지정 폴더에 바로 저장".

### 검토했다가 버린 것

| 안 | 버린 이유 |
|---|---|
| v1 서버(Express + `node:sqlite` + 디스크 사진)를 Vercel로 이전 | 서버리스는 디스크가 남지 않는다. 호스팅 DB·스토리지·인증·`user_id`가 전부 필요해진다 |
| 사진을 객체 스토리지(R2 등)에 업로드 | 비용은 작지만 0이 아니고, 로그인과 개인정보 책임이 따라온다. 사용자가 "서버에 안 올린다"로 결정 |
| Google Drive를 **주 저장소**로 | Drive는 이미지 서빙용이 아니다 (`<img src>` 불안정, OAuth 심사, 사용자가 파일을 옮기면 기록이 깨짐). 백업 대상으로만 적합 |
| 과금(구독)으로 비용 전가 | 넘길 비용이 거의 없다. 결제 채널마다 구현이 다르고 사업자 등록이 필요하다. BirdNET 라이선스와도 충돌 (4절) |

## 3. 저장 파일 형식 요구사항

계획에서 **가장 먼저 확정해야 하는 것**이다. 화면이 다루는 데이터가 곧 이 파일의 내용이라, 이게 흔들리면 화면을 다시 짠다.

- ZIP 한 개 = 기록(JSON) + 사진 파일들 + 꾸미기 초안 + 판정 이력. 브라우저에서 생성·해제 (fflate/JSZip 류).
- **형식 버전 번호를 넣는다.** 앱이 업데이트돼도 옛 파일이 열려야 한다. 원칙은 v1 꾸미기 초안과 같은 **"가산 확장만"** — 키를 바꾸거나 형태를 파괴적으로 고치지 않는다.
- **기록 ID는 UUID.** v1은 `INTEGER AUTOINCREMENT`라 두 기기에서 만든 기록이 충돌한다. 가져오기 병합 규칙도 정한다 (제안: 같은 ID는 최신 수정본 우선 → `updated_at` 필요).
- 꾸미기 초안은 v1에서 localStorage 키 `bird-journal-composer:v2:{id}`에 기록 ID로 묶여 있다. ID 체계가 바뀌므로 **초안↔기록 연결을 파일 안에서 명시적으로** 가진다. (주의: 이 키의 `v2`는 초안 스키마 버전이지 앱 v2와 무관하다.)
- v1 → v2 변환 스크립트를 염두에 둔다. v1의 `server/data/journal.db` + `server/data/photos/`를 이 ZIP 형식으로 바꿔 v2에서 "가져오기"로 연다. 사용자의 실제 기록이 거기 있다.
- 백업 알림이 기능의 일부다. "마지막 내보내기 이후 기록 N건" 배너, 탭 닫기 전 미저장 경고.

### 브라우저 저장소의 함정 (계획에 대응책 필요)

- Safari(아이폰 포함)는 7일간 방문하지 않은 사이트의 저장 데이터를 지운다. **홈 화면에 추가한 PWA는 예외** → PWA로 만들고 설치를 안내한다.
- `navigator.storage.persist()`로 영구 저장을 요청한다. 그래도 사용자가 "인터넷 기록 삭제"를 누르면 전부 사라진다.
- localStorage는 5MB 한도다. 사진은 IndexedDB나 OPFS에 넣는다.

## 4. 모델 선택과 라이선스

법률 자문이 아니다. 각 **가중치 파일**의 최신 라이선스를 계획 시점에 재확인한다 (코드 라이선스와 가중치 라이선스는 다르다).

### 라이선스 분류 (무료·비상업 배포 기준)

| 종류 | 무료 배포 | 조건 | 나중에 돈을 받으면 |
|---|---|---|---|
| Apache-2.0 / MIT / BSD | 가능 | 출처·라이선스 고지 | 가능 |
| CC BY-NC-SA (BirdNET 2.4) | 가능 | 출처 표기. 개조·파인튜닝하면 같은 라이선스로 공개 | **불가** (광고·후원도 애매) |
| AGPL-3.0 (Ultralytics YOLO) | 조건부 | 돈과 무관하게 **앱 전체 소스를 AGPL로 공개** (웹 서비스 포함) | 소스 공개 또는 Enterprise 구매 |
| 명시 없음 (BirdSet 계열 등) | **사용 금지** | — | — |

모델 파일을 내 스토리지에 올려 내려받게 하는 것은 **재배포**다. 아래 추천 모델은 모두 재배포를 허용한다. 앱에 "오픈소스·모델 출처" 화면을 둔다.

### 사진 탐지 — [권장] 후보, 최종 선택은 [미정]

v1 `detection_test/configs/models.json`에 17종이 라이선스와 함께 등록돼 있다.

1. **MediaPipe EfficientDet-Lite0** — Apache-2.0, 약 4~13MB, 공식 웹 패키지(`@mediapipe/tasks-vision`). 붙이기 가장 쉽다. 첫 선택.
2. **YOLOX-Nano/Tiny** — Apache-2.0, 약 4~20MB, onnxruntime-web. 1번 정확도가 아쉬울 때.
3. **D-FINE-N** — Apache-2.0, 십수 MB. 작은 물체에 강하지만 느리다. PC용 "정밀 모드" 후보.

제외: YOLO11/26·YOLO-World(AGPL), RF-DETR·Grounding DINO·OWLv2·Florence-2(브라우저에 너무 큼).

**타일링이 모델 종류보다 중요하다.** 2,400만 화소 사진에서 새는 작은 점이라, 통째로 320px로 줄이면 어떤 모델도 못 찾는다. `detection_test`에 `full`/`tiled` 모드가 이미 있으니, 사용자 본인 사진으로 1·2번을 tiled로 비교해 정한다. 탐지 실패 시 수동 크롭(v1 `src/lib/crop.ts`)으로 넘어가는 경로는 필수.

### 새소리 — [권장]

1. **BirdNET 2.4** — CC BY-NC-SA 4.0, 약 30~50MB, 전 세계 6천여 종(한국 새 포함). v1 `audio_detection_test`에서 기본 판정 모델로 검증했고 빠르다(RTF 0.061). 무료·비상업 조건에 맞는다. BirdNET-Analyzer 저장소에 TF.js 변환본이 있는 것으로 **기억**한다 — 미검증, 7절 스파이크 1번.
2. **BirdNET 3** — v1 설정에는 `CC BY-SA 4.0 (preview; verify release terms)`로 적혀 있다. NC가 없고 ONNX라 이식이 쉽다. 정식 조건이 확정되면 교체 후보.
3. **Perch 2** — Apache-2.0로 가장 깨끗하지만 410MB. 브라우저 불가. 쓴다면 LLM처럼 운영자 PC에서 도는 선택 옵션.
4. **YAMNet** — Apache-2.0, 14MB. 종 판별은 못 하고 "새소리 구간인가"를 거르는 앞단 필터. 선택.

제외: BirdSet 계열·Bird-MAE(라이선스 미명시), AudioProtoPNet(NC + 390MB), HawkEars(북미 종만), BirdCode(1.1GB).

### 설계 제약

- **[권장] 탐지·소리 모델은 어댑터 인터페이스 뒤에 둔다.** BirdNET 2.4→3, EfficientDet→YOLOX 교체 시 화면을 건드리지 않는다. v1 Python 쪽도 어댑터 구조다 (`docs/read/04-…` "Adapter가 필요한 이유").
- **BirdNET 2.4를 쓰는 동안은 수익화 금지**를 제약으로 명시한다. 과금하려면 모델 교체가 선행 조건.
- 모델은 **기능을 처음 쓸 때 지연 로드**하고 Cache API/OPFS에 캐시한다. 다운로드 진행률 UI. 추론은 Web Worker에서.
- 모바일 기준 **모델 하나당 50MB 이하**를 목표로 한다 (아이폰 Safari 탭 메모리 한도).
- 로컬 LLM 모델의 약관도 확인한다. 남에게 API로 제공하면 그 모델 약관이 적용된다. Qwen 계열(Apache-2.0)이 가장 깔끔하고, Llama·Gemma는 자체 약관이 있다. 어떤 모델을 쓸지는 **[미정]**.

## 5. LLM 종 판정 — v1 구조와 v2의 숙제

v1의 종 판정은 "사진 한 번 보내고 답 받기"가 **아니다.** `llm_id_test/`(Python, `birdllm`)가 다음을 한다.

- LLM이 도구 호출을 요청 → Python이 실제 웹 조회(위키백과·GBIF 등) 수행 → 결과를 대화에 추가 → 반복 (`loop.py`, `tools.py`, 판정 한 번에 도구 호출 최대 12회)
- 최종 답의 근거 요건을 별도 코드가 검사 (`verify.py`)
- 학명→국명은 로컬 표로 확인하고 **지어내지 않는다** (`data/korean_names.csv`)
- 진행 과정을 SSE로 스트리밍. 프론트에 파서가 있다 (v1 `src/lib/sse.ts`, `src/lib/species-id.ts`)
- provider 추상화가 이미 있다: `providers/openai_like.py`(LM Studio·Ollama·OpenAI 호환), `anthropic_api.py`

**[미정] 이 도구 루프를 v2에서 어디서 돌릴 것인가.** BYOK 때문에 복잡해진다.

| 안 | 장점 | 문제 |
|---|---|---|
| A. 운영자 PC에서 `birdllm`을 그대로 돌리고 터널로 노출. BYOK 키는 요청에 실어 PC로 전달 | v1 코드 재사용 최대 | PC가 꺼지면 BYOK 사용자도 판정 불가. 남의 키가 운영자 PC를 거친다 |
| B. 루프를 TypeScript로 옮겨 Vercel 함수에서 실행 | PC와 무관하게 BYOK 동작 | 함수 실행 시간 제한 (로컬 비전 모델은 장당 수~수십 초 × 여러 턴). 스트리밍 필수 |
| C. 루프를 브라우저에서 실행, 함수는 LLM 호출·웹 조회의 얇은 프록시만 | 시간 제한에서 자유롭고 키가 서버에 머물지 않는다 | 프롬프트·검증 로직이 프론트에 노출. 이식량이 가장 많다 |

어느 안이든 지킬 것:

- **기록 저장과 종 판정을 분리한다.** PC가 꺼져 있어도 기록은 저장되고, 판정은 "대기 중 → 완료 / 다시 시도" 상태로 나중에 채운다. 이 상태들을 새 UI에 처음부터 넣는다.
- **BYOK 키는 사용자 브라우저에만 둔다.** 요청 시 헤더로만 전달하고, 서버는 저장하지도 로그에 남기지도 않는다.
- LLM에는 **크롭본만** 보낸다. 캔버스를 거친 크롭은 EXIF(위치)가 빠진다.
- 판정 이력에 **어느 provider/모델이 판정했는지, 어느 크롭(`crop_box`)에 대한 것인지**를 남긴다. v1 `identification_runs`가 이미 그렇게 한다.
- v1 `server/index.js`의 `/api/llm` 프록시에는 **실측으로 확인한 SSRF 구멍을 막은 로직**이 있다 (`/api/llm/http://127.0.0.1:8798/...`가 다른 사이드카 응답을 돌려줬던 문제, 71행 부근 주석). v2 프록시로 옮길 때 같이 가져간다.
- 로그인이 없으므로 로컬 LLM 남용 방지는 IP/익명 기기 토큰 기준의 일일 한도 정도가 한계다 (우회 쉬움). GPU 한 장이라 요청은 직렬 대기열이 된다. 지인 규모에선 충분하고, 공개 규모가 되면 이 부분에만 가벼운 로그인을 붙인다.
- OpenAI 호환 API로 통일하면 나중에 로컬 → 유료 호스팅 이전이 주소·키 교체로 끝난다.

### KV 캐시(prefill 재계산) — 규칙이 아니라 **나중에 확인할 것**

배경: 사용자가 Claude Code에 로컬 모델을 물렸을 때 input 토큰이 과하게 많고 느렸던 경험이 있다. 같은 문제가 v2 판정 루프에서 생기는지 확인이 필요하다. 아래는 **결정이 아니다.** 실제 서빙 환경에서 측정한 뒤에 필요한 것만 반영한다.

코드에서 확인된 사실 (v1):

- OpenAI 호환 API는 상태가 없다. v1 루프는 턴마다 `messages` 전체(시스템 프롬프트 + 사진 + 도구 정의 + 누적된 검색 결과)를 다시 보낸다 (`llm_id_test/src/birdllm/loop.py` 125~157행). 그래서 **집계되는 input 토큰은 턴마다 누적**된다. 이것은 정상이며 그 자체로 문제는 아니다.
- 루프는 뒤에 덧붙이기만 하는 구조라 앞부분이 유지된다 — 서버가 prefix 캐시를 지원하면 재사용될 수 있는 형태다.
- 호출 한도(12회)에 걸리면 마지막 요청에서 payload의 `tools`를 뺀다 (`loop.py` 163행 → `providers/openai_like.py` 111행). 도구 정의는 보통 챗 템플릿 앞쪽에 렌더링되므로, 이 경로에서는 앞부분이 달라져 캐시가 안 맞을 **가능성**이 있다. 대안은 `tools`를 유지하고 `tool_choice: "none"`을 주는 것인데, 서버·모델이 이를 지원하는지부터 확인해야 한다.
- v1 provider는 응답의 `usage`를 읽지 않는다. 캐시가 도는지 지금은 알 길이 없다.

확인이 필요한 것 (미검증 — 일반론에 근거한 추정):

1. 사용자가 쓰는 서빙 엔진이 무엇이며(Unsloth 뒤의 실제 엔진 미확인), **prefix 캐시를 지원하는지.** 서버 로그의 실제 prefill 토큰 수(llama.cpp는 `prompt eval ... / N tokens`)나 응답의 `cached_tokens`/`timings`로 확인한다.
2. 캐시가 돌 때와 안 돌 때 판정 한 건(최대 12턴)의 소요 시간 차이. 차이가 작으면 아래 항목들은 무시해도 된다.
3. 여러 사용자의 판정이 번갈아 들어올 때 캐시가 서로 밀려나는지. 밀려난다면 판정 한 건 단위의 직렬 대기열, 슬롯 분리, 엔진 교체 중 무엇이 맞는지.
4. 사진(비전 인코더)이 요청마다 다시 처리되는지.
5. 고른 모델의 특성이 캐시 재사용을 막는지 (슬라이딩 윈도우 어텐션, 이전 턴의 생각을 히스토리에서 지우는 템플릿 등).

측정 결과 문제가 맞다면 그때 고려할 것: 시스템 프롬프트·도구 정의 고정(변하는 값은 맨 뒤 메시지로), 마지막 턴 `tools` 유지, 직렬 대기열, 도구 결과 길이 한도(v1은 `MAX_EXTRACT_CHARS = 5000`) 조정.

## 6. v1에서 가져올 것 / 두고 갈 것

v1 위치: `../bird_app v1/` (git 원격 `m-kim77/birding`, MIT). 코드 약 6,300줄. **화면과 구조는 새로 만들되 검증된 로직은 복사해 온다.** 가져올 때 테스트도 같이 가져온다 (`test/*.test.ts`, `node --test`).

### 가져올 것

| v1 파일 | 내용 | 비고 |
|---|---|---|
| `src/lib/exif.ts` | EXIF에서 GPS·촬영 시각·카메라 정보 파싱 (`exifr`) | 리사이즈 **전에** 읽어야 정보가 안 날아간다 |
| `src/lib/crop.ts` | 캔버스 크롭 | 업로드 전 리사이즈·썸네일 생성에도 재사용 |
| `src/lib/format.ts` | 표시 형식 | `formatExposure`에 선재 결함 — 아래 참고 |
| `src/lib/frame-size.ts`, `photo-slots.ts`, `map-links.ts` | 순수 로직 | 그대로 |
| `src/lib/composer-draft.ts` | 꾸미기 초안 스키마와 호환 규칙 | 아래 "이어받는 결정" 필수 |
| `src/lib/species-id.ts`, `sse.ts` | 판정 SSE 이벤트 파싱, 국명 제안 | 5절 결정에 따라 조정 |
| `src/types.ts` | 데이터 모델 출발점 | |
| `server/lib/tracklog.js` | 구글 타임라인 이동 기록 파싱·사진 시각 매칭 | 브라우저로 이전. v1의 256MB 업로드 문제가 사라진다 |
| `server/lib/place.js` | 좌표 → 장소 이름 | 외부 API 호출부만 Vercel 함수로 |
| `server/db.js`의 스키마 | 테이블·컬럼 정의와 **컬럼 주석** | 아래 "데이터 모델" |

### 두고 갈 것

- `server/index.js` (Express) — v2에 서버가 없다. SSRF 방어만 발췌.
- `src/components/SightingForm.tsx`(1,483줄), `JournalComposer.tsx`(809줄) — 새 디자인으로 다시 짠다. 안에 든 **동작 규칙은 읽고 옮긴다.**
- `detection_test/`, `audio_detection_test/`, `species_id_test/`, `llm_id_test/` — Python 실험 폴더. v2 repo에 넣지 않는다. 모델 비교·벤치마크는 계속 v1에서 한다.
- `node_modules`, `dist`, `graphify-out`, **`server/data/`(사용자의 실제 기록과 사진 — v2 repo에 절대 넣지 말 것)**.

### 데이터 모델 (v1 `server/db.js` 기준)

- `sightings`: `species_name`, `note`, `photo_path`, `lat`/`lng`, `location_source`(`exif|gps|manual|tracklog|none`), `captured_at`(UTC 'Z' 고정 폭 ISO — 사전순 = 시각순에 정렬이 기대고 있다), `captured_at_offset`(`+09:00`, 촬영지 시각 복원용), 카메라 EXIF 컬럼들(`camera_make/model`, `lens_model`, `focal_length`, `f_number`, `exposure_time`, `iso`, `raw_file_name`, `rating`), `crop_path`, `crop_box`, `detector_model`
- `crop_box`는 **정규화 좌표(0~1)**, EXIF 회전을 적용한 원본 프레임 기준이다. 픽셀로 적으면 사진을 다시 export하는 순간 의미를 잃는다. 유지할 것.
- `identification_runs`: 기록 하나에 판정 여러 건 (모델을 바꿔가며 비교). 위키백과 원문은 담지 않는다 (판정 하나가 수 MB가 된다).
- `track_points`: PK가 `(t, source, lat, lng)`다 — `source`를 빼면 같은 시각의 PATH와 rawSignals가 충돌해 정확도 있는 행이 버려진다 (실측 50행 손실). **가장 빨리 커지는 테이블**이라 저장 파일에 통째로 넣을지는 [미정].
- v2에서 바뀌는 점: ID → UUID, `updated_at` 추가, `photo_path`/`crop_path`(`/photos/<파일명>`) → 로컬 blob 참조, 사진은 큰 본(긴 변 2048px 안팎)과 썸네일(약 400px) 두 단계.
- **[권장]** sqlite-wasm + OPFS를 쓰면 v1 스키마와 SQL을 대부분 재사용할 수 있다. IndexedDB 직접 사용과 비교해 계획에서 정한다. `node:sqlite`는 동기 API라 호출부는 어차피 바뀐다.

### 이어받는 확정 결정 (v1 CLAUDE.md "꾸미기(관찰지) 확정 결정 — 다시 묻지 말 것")

- **스티커는 일단 제거** (2026-09-04). 글자만 얹는 텍스트 버튼이라 이름이 가리키는 그래픽이 없었다. 최종 결정은 아니다 — 나중에 SVG로 다시 만들지 미정. 그래서 **초안의 `stickers` 배열은 지우지 않고 보존**하고, 스티커가 든 초안을 열면 사용자에게 알린다.
- **셔터는 초 단위 실수로 저장한다.** `1/200` 표기는 표시·입력 형식일 뿐이다. `formatExposure`에 **선재 결함**이 있다 — 1초 미만을 무조건 역수로 만들어 `0.7초`·`0.8초`가 둘 다 `1/1s`, `0.4초`가 `1/3s`로 나온다. v2로 옮기면서 고친다. 왕복 오차 1% 한계를 지킬 것.
- **이름 테두리 기본은 한 겹** (2026-09-07). 두 겹은 `TitleStyle.double` 스위치로 남겼다. 되돌리지 말 것. (1) 프리셋은 `double`을 건드리지 않는다. (2) `titleStyle`은 있는데 `double` 키가 없는 초안은 **두 겹으로 읽는다** — 두 겹만 있던 시절에 저장된 것이라 기본값을 씌우면 이미 만든 관찰지 모양이 말없이 바뀐다.
- **학명(`latin`)은 삭제하지 않는다.** 종 자동 판별의 자동 입력 착지점이다. 표시를 줄이고 토글 대상으로만 둔다.
- **초안 스키마는 가산 확장만.** 새 정보는 형제 키로 더한다.
- v1 `.omc/plans/journal-composer-v2.md`(rev4, 합의 3라운드, 실행 승인 대기 상태로 끝남)에 위 항목의 수용 기준(AC-54~57, JC-0)이 있다. v2에서 꾸미기를 다시 짤 때 이 기준을 흡수한다.

### 이어받는 코드 주석 규칙 → v2 CLAUDE.md에 옮길 것

- 함수를 만들면 위에 **JSDoc 블록 필수.** 무엇을 하는지 + **실패/경계 동작**을 적는다.
- 타입·인터페이스 필드에 의미가 안 드러나면 `/** ... */`.
- "왜"가 자명하지 않은 줄에는 이유를 적는다. 특히 우회·제약·함정.
- 주석은 **한국어.** 코드가 이미 말해주는 것을 중복해 적지 않는다. 값은 의도·제약·실패 동작에 있다.

v1 CLAUDE.md의 "동시 작업 경계"(`journal.db` 다중 프로세스, 포트 5173/8787)와 graphify 규칙은 v2에 해당하지 않는다.

### 읽어 둘 v1 문서

`docs/read/00~09-*.md`(폴더별 코드 해설), `docs/기획서.md`, `docs/PRODUCT.md`, `docs/ARCHITECTURE.md`, `docs/기능별_설계기획서_v2.md`. 단, 이 문서들은 "단일 사용자·인증 없음·로컬 서버" 전제로 쓰였다.

## 7. 기술 위험 — 첫 마일스톤에서 검증할 것

안 되면 계획의 절반이 바뀐다. **디자인 작업 전에 또는 병행으로** 확인한다.

1. **BirdNET 2.4가 브라우저에서 도는가.** TF.js 변환본의 존재·크기·커스텀 레이어(멜 스펙트로그램) 동작, 폰에서의 속도와 메모리.
2. **타일 탐지가 폰에서 몇 초 걸리는가.** 2,400만 화소 사진 1장 기준, EfficientDet-Lite0 / YOLOX-Nano, WASM vs WebGPU.
3. **Vercel 함수 → 터널 → 로컬 LLM 스트리밍이 시간 제한 안에 도는가.** Hobby 플랜의 실행 시간 한도와 `maxDuration`, SSE 중계. 이때 5절 "KV 캐시" 항목의 측정(실제 prefill 토큰 수, 턴당 소요 시간)을 같이 한다.
4. **수백 장 규모에서 로컬 저장소와 ZIP 내보내기가 버티는가.** 아이폰 Safari의 저장 한도·메모리, 스트리밍 ZIP 생성 필요 여부.

## 8. 권장 진행 순서

1. 빈 Vite + React + TypeScript 프로젝트를 **바로 Vercel에 배포**해 파이프라인을 확인한다. v2는 **독립 git repo**여야 한다 (Vercel은 repo 단위 배포). 폴더·repo 이름에서 공백은 빼기를 권한다 — 현재 폴더는 `bird_app v2`.
2. 7절의 스파이크 4건.
3. **데이터 계층**: 로컬 저장소, 파일 형식(버전 포함), 내보내기/가져오기. UI 없이 테스트로 검증한다.
4. 디자인 시스템(토큰·공통 컴포넌트)과 화면 셸. v1의 거대 컴포넌트를 반복하지 않도록 처음부터 쪼갠다.
5. 기능 이식: 기록 작성(EXIF) → 목록 → 지도 → 트랙로그 → 꾸미기 → 탐지·크롭 → LLM 판정(BYOK + 로컬 폴백) → 새소리.
6. v1 → v2 데이터 변환 스크립트.

## 9. 계획서가 답해야 할 미정 사항

1. LLM 도구 루프의 실행 위치 (5절 A/B/C).
2. 로컬 DB: sqlite-wasm + OPFS vs IndexedDB.
3. 파일 형식의 구체 스키마, 가져오기 병합 규칙, `track_points` 포함 여부.
4. 탐지 모델 최종 선택 (v1 `detection_test`에서 본인 사진으로 tiled 비교 후).
5. 로컬 LLM에 올릴 모델과 그 약관, 서빙 프로그램, 터널 방식.
6. 로컬 LLM 남용 방지 수준 (IP 한도만 / 초대 코드 / 가벼운 로그인).
7. PWA 범위 (오프라인 동작을 어디까지 보장할지).
8. v2 repo 이름과 공개 여부. 공개 repo면 라이선스 (v1은 MIT). BirdNET 가중치를 repo에 넣지 않고 외부 스토리지에 두는 방식.
9. 새 디자인의 방향 — 이 대화에서는 "완전히 새롭게"만 정해졌고 구체 방향은 논의하지 않았다.

## 10. 비용 전제 (재확인 필요한 기억값)

- Vercel Hobby: 전송 월 약 100GB, 함수 호출 월 약 100만 회, **비상업 용도 한정.** 돈을 받으면 Pro(월 $20).
- Cloudflare R2: 저장 10GB 무료, **전송 무료**, 초과 시 GB당 월 약 $0.015. 카드 등록 필요.
- 로컬 LLM: API 비용 0. 전기료와 PC 상시 가동이 실비용.
- 이 구조에서는 사용자가 늘어도 운영자 비용이 거의 늘지 않는다. 늘어나는 것은 로컬 LLM 대기열뿐이다.
