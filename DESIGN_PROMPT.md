> 2026-09-22: 이 문서의 카드 **등급**(COMMON/RARE/EPIC/LEGENDARY, 별) 관련 내용은 폐기됐다 — 새에 등급을 매기지 않는다. 카드는 색 하나(사진에서 뽑거나 사용자가 고름)와 빛 효과만 갖는다 (`WORK_ORDERS.md` 작업 2). 테마 후보 등 나머지는 유효하다.

# Claude Design용 프롬프트

작성일 2026-09-22 (rev2: 테마 전환 + 자동 카드 반영). 기능 설명은 v1 코드(`../bird_app v1/src`)와
`HANDOFF.md`의 확정 사항에서, 테마 값은 Figma 시안(`ref_design/탐조일지 모바일 앱 디자인 (2)/src/themes.ts`)에서 뽑았다.

## 진행 순서 — 이 순서를 지킬 것

테마가 6개라고 화면을 6벌 만들지 않는다. **배치는 한 벌, 겉모습만 토큰으로 바꾼다.**

1. 기본 테마(도감) **하나로** 주요 화면을 만든다 → 1번 프롬프트
2. 배치가 마음에 들 때까지 고친다 (이 단계에서 다른 테마는 보지 않는다)
3. 카드 화면을 만든다 → 2번 프롬프트
4. 나머지 화면·상태 화면을 만든다 → 3번 프롬프트들
5. 토큰 표를 뽑고, 나머지 5개 테마를 **토큰 교체만으로** 만들어 대표 화면 2장씩 확인한다 → 4번 프롬프트
6. PC 레이아웃 → 5번 프롬프트

첨부("+")할 것: `themes.ts`(테마 값), 예전에 만든 Card Reveal 카드 디자인 캡처. v1 화면 캡처는 넣지 않는다.

---

## 1. 메인 프롬프트 (Mobile app design 템플릿, Design system: None)

```
탐조(새 관찰) 기록 앱 "탐조일지"(가칭)의 모바일 화면을 디자인해 줘.

[한 줄 소개]
새를 보러 다니는 사람이 자기가 찍은 사진과 녹음으로 "언제, 어디서, 어떤 새를 봤는지"를 기록하고, AI의 도움으로 종을 알아내고, 기록할 때마다 새 카드 한 장이 자동으로 만들어져 모이는 앱.

[플랫폼]
- 반응형 웹앱 하나(PWA). 웹 주소로 접속해 쓰고, 폰 홈 화면에 설치하면 앱처럼 동작한다. 나중에 같은 화면을 안드로이드 앱으로도 포장한다. 웹용·앱용을 따로 만들지 않고 같은 컴포넌트의 배치만 화면 크기에 따라 바뀐다.
- 폰이 우선이다. 폰은 현장에서 빠르게 기록·판정하는 용도, PC는 집에서 정리하고 꾸미는 용도.
- 폰은 하단 탭(가운데 큰 + 버튼), PC는 왼쪽 사이드바. 마우스를 올려야만 보이는 조작은 쓰지 않는다.
- 화면 글자는 전부 한국어.

[사용자와 상황]
- 망원 렌즈 카메라로 새를 찍는 취미 탐조인. 카메라 사진을 폰으로 옮겨 현장에서 바로 종을 찾아보고, 집에서는 보정한 사진으로 기록을 정리한다.
- 야외 햇빛 아래, 새벽 어스름, 한 손 조작, 장갑 낀 손. 대비가 높고 터치 영역이 커야 한다.

[디자인에 영향을 주는 이 앱의 특징]
- 로그인이 없다. 모든 기록과 사진은 내 기기 안에만 저장된다. "백업 파일 내려받기 / 불러오기"로 기기를 옮기거나 이어서 작성한다. 그래서 마지막 백업 이후 쌓인 기록 수가 늘 눈에 보여야 한다.
- 사진에서 새를 찾는 기능과 새소리 인식은 기기 안에서 돈다. 처음 쓸 때 수십 MB짜리 모델을 한 번 내려받으므로 진행률 화면이 필요하다.
- AI 종 판정은 수십 초에서 몇 분이 걸리고, 판정 서버가 쉬고 있을 때도 있다. 그래서 기록은 먼저 저장되고 판정은 "대기 중 → 진행 중 → 완료 / 다시 시도"로 나중에 채워진다.

[기능]
1. 기록하기 — 사진을 고르면 촬영 시각, 위치, 카메라 설정(카메라·렌즈·조리개·셔터·ISO·초점거리)이 자동으로 채워진다. 위치는 사진 정보 → 내 이동 기록(구글 타임라인 파일) → 현재 위치 → 지도에서 직접 찍기 순으로 정해지고, 어디서 온 위치인지 표시한다. 종 이름은 검색해서 고르며 전에 본 종이 먼저 나온다. 메모와 별점.
2. 새 찾기와 자르기 — 사진에서 새를 자동으로 찾아 상자로 표시한다. 여러 마리면 하나를 고른다. 고른 상자대로 잘라 판정에 쓴다. 못 찾으면 손으로 직접 자른다.
3. AI 종 판정 — 잘라 낸 사진을 AI에 보낸다. AI는 특징을 관찰하고, 후보를 세우고, 자료를 검색해 근거를 모은 뒤 "확정" 또는 "후보를 좁힘"으로 답한다. 이 과정이 단계별로 실시간 표시된다. 결과에는 한국어 이름, 학명, 근거 요약, 참고 사진, 출처 링크가 있고 "이 이름으로 기록에 넣기" 버튼이 있다. 같은 사진을 여러 번 판정한 이력을 나란히 비교할 수 있다.
4. 새소리 — 현장에서 녹음하거나 파일을 넣으면, 소리 그림(스펙트로그램) 위에 시간 구간별로 종 후보와 신뢰도가 표시된다. 결과를 기록으로 저장한다.
5. 새 카드 — 기록을 저장하면 그 새의 카드가 자동으로 만들어진다 (자세한 내용은 다음 요청에서).
6. 내 기록 / 내 새 도감 — 사진 썸네일·종 이름·날짜·장소가 보이는 목록, 검색과 필터, 기록 상세. 모은 카드를 격자로 보는 도감 보기.
7. 지도 — 관찰 위치를 핀으로 보고 장소별로 모아 본다.
8. 관찰지 꾸미기 — 기록 한 건을 한 장의 관찰지로 공들여 만드는 상세판. 사진 배치, 새 이름 표제, 항목 켜고 끄기와 순서, 판형(인스타그램 4:5, 1:1, 4×6 인화, A5), 인쇄·PDF·이미지 내보내기.
9. 홈 — 이번 달 기록 수, 지금까지 본 종 수, 최근 기록, 빠른 기록 버튼, 백업 알림.
10. 설정 — 화면 테마 고르기, 백업 내려받기/불러오기, AI 연결(내 API 키 입력, 없으면 기본 제공 AI 사용), 내려받은 모델 관리(용량), 오픈소스·모델 출처.

[테마 — 중요]
이 앱은 설정에서 화면 테마를 바꿀 수 있다. 테마는 6가지다.
1. 도감 (기본) — 크림색 종이 바탕, 올리브 녹색, 짙은 갈색 글씨, 각진 카드. 자연 도감·필드 노트 느낌.
2. 숲속 — 옅은 녹색 기운의 밝은 바탕, 숲 녹색과 금색 포인트, 둥근 카드.
3. 귀여운 — 따뜻한 파스텔, 하늘색과 민트, 아주 둥근 카드.
4. 심플 / 모던 — 한 쌍이다. 심플은 흰 바탕의 미니멀 라이트, 모던은 짙은 남색 다크. 기기의 라이트/다크 설정을 따라 자동으로 바뀔 수 있다.
5. 유리 — 짙은 보라·남색 그라데이션 위의 반투명 카드.
6. 팝 — 노랑·빨강의 볼드한 색, 굵은 검은 테두리, 그림자 없음.
(각 테마의 색 값은 첨부한 themes.ts를 따른다.)

테마 규칙:
- **배치는 모든 테마에서 완전히 같다.** 테마가 바꾸는 것은 색, 모서리 반경, 그림자, 테두리, 글꼴, 그리고 화면 배경 장식 한 가지뿐이다. 테마마다 다른 컴포넌트나 다른 문구를 만들지 않는다.
- **지금은 "도감" 테마 하나로만 디자인한다.** 다른 테마는 나중에 색 값만 바꿔서 만든다.
- 모든 테마에서 본문 글자와 바탕의 대비는 4.5:1 이상. 옅은 회색·옅은 갈색 글씨 금지 (첨부한 "귀여운" 테마의 보조 글씨는 너무 옅으니 더 진하게 고쳐서 쓴다).
- 새 사진에는 어떤 테마에서도 색을 입히거나 필터를 걸지 않는다.
- 성공·경고·오류 색은 테마가 달라도 같은 뜻으로 읽혀야 한다.

[도감 테마의 글자]
- 새 이름과 화면 제목만 명조(세리프), 학명은 이탤릭. 버튼·입력·목록·본문은 읽기 쉬운 고딕. 작은 글씨에 명조를 쓰지 않는다.
- 가장 작은 글씨도 12px 이상. 터치 영역 최소 48px.

[피할 것]
- 이모지를 아이콘으로 쓰지 않는다. 아이콘은 한 가지 선 아이콘 세트로 통일하고, 새 자리에는 사진 썸네일을 쓴다.
- 만화풍 새 캐릭터, 과한 그라데이션, 스톡 사진 같은 분위기.

[이번에 만들 화면 — 폰 기준 6장, 도감 테마]
1. 홈
2. 기록하기 (사진을 고른 직후, 시각·위치·촬영 정보가 자동으로 채워진 상태)
3. 새 찾기와 자르기 (새 두 마리가 상자로 표시된 상태)
4. AI 종 판정 (진행 중인 단계 표시 + 아래에 완료된 결과 카드)
5. 기록 상세
6. 설정 > 화면 테마 (6개 테마를 작은 미리보기로 고르는 화면)

사진 자리에는 실제 새 사진 대신 자리 표시를 쓰되 망원으로 찍은 가로 사진 비율(3:2)을 지켜 줘.
```

## 2. 카드 프롬프트 (Card Reveal 디자인 캡처를 첨부하고)

등급을 **무엇으로 나눌지는 아직 미정**이다 (아래 "미정 사항"). 그래서 여기서는 4단계의 **시각 차이만** 디자인하고, 등급 이름은 나중에 바꿀 수 있는 글자로 둔다.

```
같은 앱의 "새 카드" 기능을 디자인해 줘. 첨부한 카드 디자인을 기준으로 삼는다.

[카드가 무엇인가]
- 기록을 저장하면 그 새의 카드 한 장이 자동으로 만들어진다. 사용자는 아무것도 꾸미지 않아도 된다.
- 카드에는 새 사진(자른 사진), 한국어 이름, 학명(이탤릭), 관찰 날짜, 장소, 촬영 정보 한 줄, 등급 표시가 들어간다.
- 카드는 이미지로 저장하거나 공유할 수 있다. 비율은 인스타그램에 맞는 4:5.
- **카드 디자인은 앱 테마와 무관하게 항상 같다.** 공유된 카드가 어느 사용자 것이든 같은 앱의 카드로 보여야 한다.

[등급 — 4단계]
- 1단계부터 4단계로 갈수록 테두리·바탕·광택이 화려해진다. 1단계는 차분하고, 4단계는 한눈에 특별해 보인다.
- 등급 이름은 나중에 바꿀 수 있게 작은 라벨 한 곳에만 쓴다. 지금은 임시로 "1단계 / 2단계 / 3단계 / 4단계"로 적는다.
- 등급이 달라도 카드의 배치와 정보는 같다.

[도장]
- 등급과 별개로, 사실 정보를 작은 도장으로 찍을 수 있다: "천연기념물", "멸종위기", "길잃은새", "첫 만남".

[위치 가리기]
- 보호가 필요한 종의 카드는 장소를 시·군 단위까지만 적거나 "위치 비공개"로 표시한다. 이 상태의 카드도 디자인해 줘.

[카드가 나오는 순간]
- 처음 본 종을 기록했을 때만 카드가 뒤집히며 나타나는 연출이 나온다. 이미 본 종은 연출 없이 바로 카드를 보여 준다.
- 움직임 줄이기 설정을 켠 사용자에게는 연출 없이 보여 준다.

[만들 화면 — 폰 기준]
1. 카드 4단계를 나란히 놓은 비교 (같은 새, 등급만 다르게)
2. 기록 저장 직후 — 새 카드가 나온 화면 (저장·공유·관찰지 꾸미기로 가기 버튼)
3. 처음 본 종의 등장 연출 (시작·중간·끝 3장면)
4. 내 새 도감 — 모은 카드를 격자로 보는 화면. 아직 못 본 종은 빈 자리로 보이지 않게 한다 (못 본 종 목록을 채우라고 압박하지 않는다).
5. 카드 크게 보기 + 도장이 찍힌 카드 + 위치 비공개 카드
```

## 3. 이어서 요청할 것 (한 번에 하나씩, 도감 테마로)

```
같은 디자인으로 상태 화면들을 만들어 줘: 기록이 하나도 없는 첫 화면, 모델 첫 다운로드 진행률(새 찾기·새소리), 판정 대기 중, 판정 서버가 쉬는 중 + 다시 시도, 새를 못 찾음 → 직접 자르기로 전환, 오프라인, "마지막 백업 이후 기록 12건" 배너, 백업 없이 닫으려 할 때 경고, 백업 불러오기 결과(추가 8건·갱신 3건).
```

```
같은 디자인으로 새소리 화면을 만들어 줘: 녹음 중(실시간 소리 그림), 분석 결과(소리 그림 위 시간 구간별 종 후보와 신뢰도), 결과를 기록으로 저장.
```

```
같은 디자인으로 내 기록 목록(검색·필터 포함)과 지도 화면(핀, 장소별 모아 보기)을 만들어 줘.
```

```
같은 디자인으로 관찰지 꾸미기 화면을 만들어 줘. PC 화면 기준: 가운데에 관찰지 미리보기, 옆에 편집 패널(판형 선택, 사진 배치, 이름 표제 스타일, 항목 켜고 끄기·순서). 폰에서는 미리보기 아래에 편집 패널이 시트로 올라오는 형태.
```

## 4. 토큰 표와 나머지 테마 (배치가 확정된 뒤)

```
지금까지 "도감" 테마에 쓴 디자인 토큰을 표로 정리해 줘: 색(역할별 이름과 값 — 화면 바탕, 카드 바탕, 글자 3단계, 주 색, 강조 색, 표면, 테두리, 탭, 태그, 성공·경고·오류), 글꼴과 크기 단계, 간격 단계, 모서리 반경, 그림자, 테두리 두께, 배경 장식. 그리고 공통 컴포넌트 목록(버튼, 입력, 카드, 탭, 시트, 배너, 진행률 등)과 각 컴포넌트의 상태.
```

```
위 토큰 표의 "이름"은 그대로 두고 "값"만 바꿔서 나머지 테마 5개(숲속, 귀여운, 심플/모던, 유리, 팝)의 토큰 표를 만들어 줘. 값은 첨부한 themes.ts를 출발점으로 하되, 본문 대비 4.5:1을 못 넘는 값은 고쳐 줘. 새 토큰 이름을 만들어야만 표현되는 차이가 있으면 만들지 말고 따로 알려 줘.
```

```
홈과 기록 상세 두 화면을 6개 테마(심플·모던은 각각)로 나란히 보여 줘. 배치는 절대 바꾸지 말고 토큰 값만 바꿔서.
```

## 5. PC 레이아웃

```
주요 화면(홈, 기록하기, 내 기록, 기록 상세, 내 새 도감)을 PC 레이아웃으로 만들어 줘. 왼쪽 사이드바, 내 기록은 사진 격자 + 목록·상세 2단, 본문 폭 상한. 폰 화면과 같은 컴포넌트를 쓰고 배치만 바꾼다.
```

---

## 미정 사항 (디자인과 별개로 정해야 하는 것)

- **카드 등급을 무엇으로 나눌지.** 후보: (A) 새 자체의 희귀도 — 한국조류목록의 도래 현황, 환경부 멸종위기 목록, 천연기념물 목록을 매핑. 지역·계절에 따라 달라지는 것을 전국 기준으로 단순화하게 되고, 각 목록의 이용 조건 확인이 필요하다. (B) 나에게 얼마나 특별한가 — 첫 만남 / 올해 첫 / 다시 만남. 외부 자료가 필요 없고 오프라인에서 동작한다. (C) 등급은 B, 객관적 사실은 도장으로. 2번 프롬프트는 C를 전제로 도장을 넣어 두었다.
- **등급 이름.** common/rare/epic/legendary(게임 용어) vs 탐조인의 말(흔한 새/드문 새/희귀한 새/길잃은새, 또는 첫 만남/올해 첫/다시 만남).
- **보호가 필요한 종의 목록**을 어디서 가져올지.
