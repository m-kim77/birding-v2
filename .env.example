# 기본 제공 AI(운영자의 로컬 LLM) 연결. Vercel에서는 프로젝트 환경변수로 넣는다 — 이 값들은 브라우저로 나가지 않는다.
# 로컬 개발: 이 파일을 .env.local 로 복사해서 쓴다 (.env* 는 git에 올라가지 않는다).
LOCAL_LLM_URL=http://127.0.0.1:1234/v1
LOCAL_LLM_KEY=
LOCAL_LLM_MODEL=qwen3.8-flash-next
