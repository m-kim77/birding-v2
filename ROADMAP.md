# 작업 순서 (ROADMAP)

**무엇을 어떤 순서로 하는지, 어디까지 했는지는 이 파일 하나만 정한다.** 다른 문서는 순서를 정하지 않는다.
작업을 시작하기 전에 이 파일을 읽고, 작업이 끝나면 상태를 바꾼다. 마지막 갱신: 2026-09-26.

## 문서마다 맡은 일

| 문서 | 맡은 일 |
|---|---|
| **ROADMAP.md** (이 파일) | 순서 · 진행 상태 · 결정 대기 · git 올리는 법 |
| [`WORK_ORDERS.md`](WORK_ORDERS.md) | 작업마다의 자세한 지시 — 왜 · 무엇이 바뀌나 · 닿는 파일 · 검증. 작업을 시작할 때 쓴다 |
| [`FEATURES.md`](FEATURES.md) | 탐조인에게 필요한 기능 전체 목록과 이유 (아이디어 창고 — 순서는 여기서 정하지 않는다) |
| [`README.md`](README.md) | 지금 되는 것 / 없는 것, 실행 · 배포 |
| [`CLAUDE.md`](CLAUDE.md) | 매 세션 지킬 규칙 |
| [`HANDOFF.md`](HANDOFF.md) · [`BUTTONS.md`](ref_design/design_v01/BUTTONS.md) | 결정의 배경 · 버튼마다의 존재 이유 |

## 지금 순서

**번호는 이름표다. 순서는 표의 위에서 아래로다.** "작업 N 해줘"로 시작한다. 한 번에 하나만 하고, 앞 작업을 올려 확인한 뒤 다음으로 간다.
상태: ⬜ 안 함 · 🔨 하는 중 · ✅ 끝 (날짜)

| 순서 | 번호 | 할 일 | 상태 | 브랜치 | 자세한 지시 |
|---|---|---|---|---|---|
| 1 | 0 | 이 순서 문서와 2차 작업 지시를 올린다 | ✅ (2026-09-26) | main에 바로 | — |
| 2 | 6 | 상세 '수정'이 옛 값으로 덮어써 AI 이름이 지워지는 문제 | ✅ (2026-09-26) | `fix/work-order-6` | WORK_ORDERS 작업 6 |
| 3 | 7 | 저장이 끝까지 된 뒤에만 성공으로, 기록과 사진을 한 번에 | ⬜ | `fix/work-order-7` | 작업 7 |
| 4 | 8 | 백업 불러오기: 기록마다 검사, 기록과 사진을 한 번에 | ⬜ | `fix/work-order-8` | 작업 8 |
| 5 | 확인 | 배포 AI가 실제 사진으로 끝까지 되는지 (코드 없음) | ⬜ | — | WORK_ORDERS "확인" |
| 6 | 9 | 기본 제공 AI 통로 보호 — **대시보드 규칙은 지금 걸어도 된다** | ⬜ | `fix/work-order-9` | 작업 9 |
| 7 | 10 | 장소 이름 조회 다듬기 | ⬜ | `fix/work-order-10` | 작업 10 |
| 8 | 11 | 문서를 실제와 맞추기 | ⬜ | main에 바로 | 작업 11 |
| 9 | 12 | 저장한 기록의 시각·위치 고치기 | ⬜ | `feat/work-order-12` | 시작할 때 쓴다 (메모: WORK_ORDERS "다음 기능 메모") |
| 10 | 13 | 폰 뒤로가기 연동 | ⬜ | `feat/work-order-13` | 〃 |
| 11 | 14 | 위치 숨기기 스위치 + "무엇이 어디로 가나요" 안내 | ⬜ | `feat/work-order-14` | 〃 |
| 12 | 15 | 저장 공간·백업 상태 표시 | ⬜ | `feat/work-order-15` | 〃 |
| 13 | 시험 | 구글 드라이브 이틀 시험 (코드 거의 없음) — 실제 사이트에서 구글 로그인 → 파일 하나 올리고 받기 | ⬜ | — | 아래 "저장 방식 메모" |
| 14 | 19 | **구글 드라이브 동기화 (PC 먼저)** — 저장하면 드라이브에 올리고, 로그인하면 드라이브에서 받아 합친다. 폰은 나중 | ⬜ | `feat/work-order-19` | 〃 |
| 15 | 16 | 오프라인 (서비스 워커) | ⬜ | `feat/work-order-16` | 〃 |
| 16 | 17 | v1 기록 가져오기 — 8·10이 끝난 뒤 | ⬜ | `feat/work-order-17` | 〃 |
| 17 | 18 | 'AI 판정 · 확정' 표현 바꾸기 — 어떤 말로 할지 먼저 정한다 | ⬜ | `feat/work-order-18` | 〃 |

**그 뒤 (순서 미정, 필요할 때 위 표로 올린다)**: 사진 없는 빠른 기록·개체 수 · 한 번의 탐조를 묶기(탐조 세션) · 기간·장소 필터와 올해 본 종 · 보호종·천연기념물 자료 · 새소리(BirdNET) · 모델을 바꾸기 전 비교용 대표 사진 세트(사진은 저장소 밖).

**이 순서의 이유**: 기록이 사라지거나 앱이 멈추는 문제(6~8)가 먼저다. 6이 가장 자주 겪고 가장 작다. 7이 만드는 "한 번에 쓰기"를 8이 쓴다. 9·10은 링크를 남에게 알리기 전에. 기능은 "틀린 값을 영원히 못 고치는 것(12)" → 현장에서 매일 걸리는 것(13) → 새와 사람을 지키는 것(14) 순이다. 16은 가장 크고 잘못 만들면 옛 판이 계속 뜨므로 작은 것들 뒤에 둔다.

## 결정 대기 — 정해지면 위 표에 줄로 들어간다

| 무엇 | 선택지 | 무엇을 막고 있나 | 근거 |
|---|---|---|---|
| ~~저장 방식 (작업 5)~~ | **정함 (2026-09-26): 기기가 원본 + 구글 드라이브가 사본.** 운영자 서버(Vercel Blob·Supabase)에는 저장하지 않는다 | → 작업 19 | 아래 "저장 방식 메모" |
| ~~로그인 (구글·네이버)~~ | **정함 (2026-09-26): 구글만, 드라이브 연결용.** 네이버·서버 로그인은 안 한다. AI 남용은 작업 9로 막는다 | → 작업 19 | 아래 "로그인 메모" |
| 작업 8: 깨진 기록이 섞인 백업 | 깨진 것만 건너뛰기(권장) / 파일 전체 거절 | 작업 8 | WORK_ORDERS 작업 8 |
| 작업 18: '확정'을 무엇이라 보여 줄지 | 예: "AI 의견 · 한 종" / "유력 후보" | 작업 18 | WORK_ORDERS "다음 기능 메모" |
| 새소리의 자리 | FEATURES에서는 1순위였다. 저장 안정성과 12~14 뒤를 권한다 | — | FEATURES.md |

## 저장 방식 메모 (2026-09-26 결정)

**결정**: 기록·사진은 지금처럼 기기에 두고(원본), 사용자 자신의 구글 드라이브에 사본을 둔다. **운영자 서버에는 저장하지 않는다** — CLAUDE.md의 결정은 그대로다. **1차는 PC만** 연동한다. 폰은 나중이다.

왜 드라이브인가 (같은 날 비교):
- 서버 저장(Vercel Blob·Supabase)은 무료 1GB를 **모든 사용자가 나눠 쓴다** — 전체 합쳐 기록 수백~천 건이면 차고, 넘으면 Vercel은 30일 차단(무료 한도를 프로젝트 전체가 함께 써서 AI 판정까지 멈출 수 있다), Supabase는 업로드 차단 + 1주일 안 쓰면 정지, 유료는 월 $25부터. 남의 사진·촬영 좌표를 내 창고에 모으는 것이라 개인정보보호법·위치정보법 부담도 생긴다 (작업 5).
- 드라이브는 사용자마다 자기 15GB, 운영자 비용 0원, 서버 코드 없음, 데이터가 운영자에게 오지 않는다. Vercel에서도 된다 (작업 5에서 확인).

**작업 19의 모양** (시작할 때 WORK_ORDERS에 자세히):
- 저장·고침·삭제 → 기기 DB에 먼저(작업 7의 한 트랜잭션) → 올릴 일을 줄에 세워 뒤에서 드라이브의 앱 폴더에 올린다 (기록은 JSON, 사진은 파일).
- 로그인(=드라이브 연결) → 앱 폴더 목록을 읽어 새 것·바뀐 것을 받아 기기 DB에 합친다 — 합치는 규칙은 백업 불러오기의 `planMerge` 그대로(같은 기록은 더 최신이 이김). 기록·작은 사진 먼저, 큰 사진은 열 때.
- 로그인이 **1시간마다 풀린다** → 줄에 남은 것은 두었다가 "드라이브 다시 연결" 버튼 한 번에 이어 올린다. 조용히 갱신하는 길이 브라우저에는 없다.
- 사진은 숨김 폴더(appDataFolder)가 아니라 **보이는 폴더**에 — 숨김 폴더는 사용자가 "앱 데이터 삭제"로 통째로 날릴 수 있다. 드라이브 주소는 `<img>`에 못 쓰므로 받아서 기기 DB에 넣는다.
- 설정 화면: 연결/해제, 마지막 동기화 시각, 올리지 못한 건수. 기존 ZIP 백업은 그대로 둔다 (드라이브를 안 쓰는 사람의 길).
- 구글 클라우드: OAuth 클라이언트 만들고 실제 사이트 주소와 `http://localhost:5173`을 "승인된 JavaScript 원본"에, 동의 화면을 **"프로덕션"으로 게시** ("테스트"면 7일마다 로그인이 풀린다). 권한은 `drive.file`(앱이 만든 파일만) — 비민감 등급이라 앱 심사 없음. **미리보기 주소에서는 로그인이 안 된다.**
- 폰으로 넓힐 때 볼 것: 아이폰 홈 화면 앱의 로그인 팝업(페이지 이동 방식 필요), 폰 저장 공간.

**이틀 시험(순서 13)에서 확인할 것**: 실제 사이트에서 구글 로그인 → 앱 폴더에 파일 하나 올리기 → 다른 브라우저에서 로그인해 받기 → 1시간 뒤 다시 올릴 때 어떻게 풀리는지. 여기서 막히면 작업 19를 시작하지 않는다.

## 로그인 메모 (구글·네이버, 2026-09-26 조사 — 결정은 위 "저장 방식 메모")

**로그인 버튼 자체는 중간 난이도다. 어려움은 "로그인으로 무엇을 하느냐"가 정한다.** 그리고 지금 문서의 결정(FEATURES "계정·로그인은 넣지 않는다", CLAUDE.md "사진과 기록을 서버에 저장하지 않는다")을 바꾸는 일이다.

| 목적 | 로그인 말고 더 필요한 것 | 난이도 | 바뀌는 결정 · 법 |
|---|---|---|---|
| **A.** 기본 제공 AI를 로그인한 사람만 쓰게 (사람마다 하루 N건) | 사람별 사용량을 세는 작은 저장소 (예: Upstash 무료) | 중간 — 며칠 + 네이버 검수 대기 | FEATURES의 "로그인 안 함". 기록은 여전히 기기에만. 개인정보 처리방침 필요 |
| **B.** 기록·사진을 서버에 두고 폰↔PC 이어 쓰기 | DB · 사진 저장소 · 동기화 · 충돌 처리 · 옛 기록 옮기기 | 어려움 — 몇 주 | CLAUDE.md의 "서버에 저장 안 함". 개인정보보호법 + **위치정보법**(촬영 좌표) — 만들기 전에 위치정보지원센터 문의 (작업 5) |
| **C.** 구글 드라이브에 백업 (작업 5의 다른 안) | 구글 로그인(드라이브 권한)뿐, 서버 코드 없음 | 중간 | 우리 서버에 모이지 않는다. 네이버는 해당 없음 |

공통으로 알아 둘 것:
- Vercel이 로그인을 주는 것은 아니다 — 앱에 인증 라이브러리를 넣는다. 후보는 Better Auth(Auth.js가 합쳐진 곳): 구글·네이버가 들어 있고, DB 없이 쿠키만으로 로그인 상태를 둘 수도 있다. 로그인 경로(`/api/auth/...`)용 함수와 개발 서버(`dev/apiPlugin.ts` — 지금은 `/api/한단어`만 받는다)를 고쳐야 한다.
- 구글: 기본 권한(openid · email · profile)만이면 앱 심사는 없다. 동의 화면에 앱 이름·로고를 띄우려면 브랜드 인증 — 공개 홈페이지, **같은 도메인의 개인정보처리방침**, Search Console 도메인 소유 확인.
- 네이버: 등록하면 '개발 중' 상태라 **등록한 테스트 아이디만** 로그인된다. 모두에게 열려면 검수(로그인 과정·제공 정보를 쓰는 곳 캡처, 로고 등)를 통과해야 한다.
- 로그인 주소는 고정 주소만 등록되므로 **Vercel 미리보기 주소에서는 로그인이 안 된다.** 아이폰 홈 화면 앱은 팝업 대신 페이지 이동 방식이 필요하다는 보고가 있다.
- 이메일·이름을 받으면 사이트에 개인정보 처리방침(무엇을·왜·얼마나 보관)을 둔다. 받는 정보를 고유 번호 정도로 줄이면 부담이 준다.

**권장**: 목적부터 정한다. A라면 작업 9(요청 제한 — 무료, 코드 적음)로 먼저 막아 보고, 부족할 때 로그인을 더한다 — 구글 먼저, 네이버는 검수가 붙으니 그다음. B·C는 작업 5 결정과 한 몸이다.

## git에 올리는 법

한 작업 = 브랜치 하나. 커밋 하나 = 변화 하나(되돌리기 쉬운 단위).

```bash
git switch main && git pull                  # 시작: 최신 main에서
git switch -c fix/work-order-6               # 작업 브랜치
# … 고치기 → npm run check → 커밋 (여러 번) …
git switch main
git merge --no-ff fix/work-order-6 -m "Merge branch 'fix/work-order-6' — 작업 6: …"
git push origin main                         # = 실제 사이트 배포
git branch -d fix/work-order-6
```

- **커밋 전마다 `npm run check`**, 합치기 전에 브라우저 확인.
- 커밋 제목: `종류(범위): 무엇을 — 왜 (작업N)`. 종류: `fix` 고침 · `feat` 새 기능 · `refactor` 동작은 그대로 구조만 · `test` · `docs` · `chore`.
- 마지막 커밋은 `docs: 작업 N 완료 기록` — WORK_ORDERS에 "완료(날짜)"와 계획과 다르게 한 것, 이 파일의 상태, README 표.
- **main에 올리는 것이 곧 배포다.** 올리기는 사용자가 확인한 뒤("올려줘")에 한다. 올린 뒤 실제 사이트에서 한 번 본다.
- 폰에서 먼저 보고 싶으면 브랜치를 올린다(`git push -u origin fix/work-order-6`) → Vercel 미리보기 주소. 미리보기에는 기본 제공 AI 환경변수가 없어 판정은 안 된다.
- 문서만 바꾸는 작업(0 · 11)은 브랜치 없이 main에 바로 커밋해도 된다 (지금까지도 그랬다).
- 브랜치를 겹쳐 두지 않는다 — 7과 8이 같은 파일을 만진다. 앞 작업을 올린 뒤 다음 브랜치를 만든다.
- **되돌리기**: Vercel → 프로젝트 → Instant Rollback (무료 요금제는 **바로 직전 배포로만**). 되돌린 뒤에는 새로 올려도 자동 배포가 멈춰 있으니, 고쳐 올린 뒤 **Undo Rollback**을 누른다. 코드는 `git revert -m 1 <합친 커밋>`.

### 작업별 커밋 계획

| 작업 | 브랜치 | 커밋 (예정) |
|---|---|---|
| 0 | main | `docs: 순서 문서(ROADMAP.md)와 2차 작업 지시 6~11 — 순서는 한 곳에서만` |
| 6 | `fix/work-order-6` | `fix(records): '수정'을 누를 때마다 지금 기록으로 이름·메모를 채운다 (작업6)` → `docs: 작업 6 완료 기록` |
| 7 | `fix/work-order-7` | `refactor(db): 쓰기가 트랜잭션 확정(complete)까지 기다린다 (작업7)` → `fix(record): 기록과 사진을 트랜잭션 하나로 저장·삭제, 용량 부족 한국어 안내 (작업7)` → `docs: 작업 7 완료 기록, CLAUDE.md에 한 트랜잭션 규칙` |
| 8 | `fix/work-order-8` | `feat(backup): normalizeSighting — 기록마다 검사하고 빈칸은 기본값, 테스트 (작업8)` → `fix(backup): 복원을 기록 단위 트랜잭션으로, 건너뛴 수 안내, 읽을 때도 빈칸 채우기 (작업8)` → `docs: 작업 8 완료 기록` |
| 확인 | — | 커밋 없음 (운영 문서는 저장소 밖 `../v2 docs/`) |
| 9 | `fix/work-order-9` | (필요하면) `refactor(identify): 도구 정의를 순수 파일로 분리 (작업9)` → `fix(api): 기본 제공 AI는 서버가 정한 지시문·도구만, 길이·그림·첫 응답 시간 상한 (작업9)` → `fix(identify): 요청 제한(429) 한국어 안내 (작업9)` → `docs: 작업 9 완료 기록, 요청 제한 규칙 안내` |
| 10 | `fix/work-order-10` | `fix(place): 같은 좌표 다시 묻지 않기·1초 간격·User-Agent 연락처 (작업10)` → `docs: 작업 10 완료 기록, 대량 처리 규칙` |
| 11 | main | `docs: README·FEATURES를 실제와 맞춤 (작업11)` |
| 12~ | `feat/work-order-N` | 첫 커밋 `docs: 작업 N 지시` (WORK_ORDERS에 자세히) → 기능 커밋들 → `docs: 작업 N 완료 기록` |
| 19 | `feat/work-order-19` | `docs: 작업 19 지시` → `feat(drive): 구글 로그인·앱 폴더 올리기/받기 (작업19)` → `feat(sync): 올릴 일 줄 세우기·받아서 합치기·설정 카드 (작업19)` → `docs: 작업 19 완료 기록, README 표` |

## 새 기능을 더할 때

- "ROADMAP에 ○○ 추가해줘"라고 하면: (1) 위 표의 알맞은 자리에 한 줄 — **다음 빈 번호**를 받는다 (2) FEATURES.md에 없던 기능이면 이유와 함께 한 줄 (3) CLAUDE.md의 확정 결정(서버 저장 안 함 · 등급 없음 · 한 장씩 등)과 부딪히면 표에 넣지 않고 "결정 대기"로.
- 자세한 지시는 그 작업을 **시작할 때** WORK_ORDERS.md에 쓴다. 미리 써 두면 코드가 바뀌어 낡는다.
- 순서를 바꾸려면 표의 줄만 옮긴다 (번호는 그대로).
