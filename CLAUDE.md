# bird_app v2

탐조 기록 웹앱의 Vercel 배포용 재작성. 디자인부터 새로 만든다.

- 실행·구조·배포·"지금 되는 것 / 없는 것": [`README.md`](README.md)
- 지금 할 순서·진행 상태·git에 올리는 법: [`ROADMAP.md`](ROADMAP.md). **작업을 시작하기 전에 읽는다. 순서는 이 파일만 정한다.** 작업마다의 자세한 지시는 [`WORK_ORDERS.md`](WORK_ORDERS.md).
- 결정의 배경·근거·미정 사항: [`HANDOFF.md`](HANDOFF.md). 계획서가 생기면 계획서가 우선한다.
- 버튼마다의 존재 이유: [`ref_design/design_v01/BUTTONS.md`](ref_design/design_v01/BUTTONS.md). **동작하지 않는 버튼을 미리 만들어 두지 않는다.**
- 끝내기 전에 `npm run check` (타입 · 테마 대비 · 파일 크기 · 테스트).
- v1은 `../bird_app v1/`에 있다. **참조·복사 전용이다. v2 작업 중에 v1 파일을 고치지 않는다.**
- 계획서에서 새로 확정되는 결정 중 **매 세션 지켜야 하는 것만** 이 파일에 추가한다. 배경 설명은 넣지 않는다.

## 확정된 구조 — 다시 묻지 말 것

- **사진과 기록을 서버에 저장하지 않는다.** 데이터는 사용자 브라우저에 있고, 파일(ZIP)로 내려받아 다시 올려서 이어 쓴다. 서버에 사용자 데이터를 쌓는 방향의 제안을 하지 않는다.
- **무료·비상업 배포.** 과금·광고를 넣지 않는다.
- **LLM은 두 갈래.** 사용자가 자기 API 키를 넣으면 그것을 쓰고, 없으면 운영자 PC의 로컬 LLM(OpenAI 호환)을 쓴다.
  내 키 요청은 브라우저에서 그 서비스로 **직접** 가고, 기본 제공 AI만 `api/llm.ts`를 거친다. `api/llm.ts`가 요청에서 주소를 받게 고치지 말 것 — 열린 프록시(SSRF)가 된다.
- **판정 루프는 브라우저에서 돈다** (`features/identify/loop.ts`). 도구는 `tools/` 폴더에 하나씩, 목록은 `tools/index.ts`. 시스템 프롬프트·도구 정의·도구 순서는 고정이다 — 바꾸면 LLM 서버의 KV 캐시가 깨진다. 변하는 값은 사용자 메시지에 넣는다.
- **사용자 API 키는 브라우저에만 둔다.** 서버에 저장하지 않고 로그에도 남기지 않는다.
- **새 탐지·새소리 모델은 사용자 기기(브라우저)에서 추론한다.** 사진·녹음을 추론하려고 서버로 보내지 않는다.
- **LLM에는 캔버스에서 다시 인코딩한 그림만 보낸다** (자른 영역, 또는 사용자가 자르지 않았으면 사진 전체 — 어느 쪽이든 1024px, `record/savePhotos.ts imageForAI`). 원본 파일을 그대로 보내지 않는다 — 위치 EXIF가 따라간다.
- **판정의 출처 링크·참고 사진은 도구가 실제로 돌려준 값에서만 모은다** (`identify/loop.ts collectReference`). 모델의 답에 적힌 주소를 링크로 만들지 않는다 — 지어낼 수 있다.
- **v1의 `server/data/`(사용자의 실제 기록·사진)와 모델 가중치 파일을 이 repo에 넣지 않는다.**
- **이동 기록(구글 타임라인)은 브라우저 안에서만 읽어 기기(`data/tracks.ts`)에 둔다.** 서버로 보내지 않고, 백업 ZIP에 넣지 않고, 좌표를 로그에 찍지 않는다. 실제 타임라인 파일과 실제 좌표를 repo(테스트·주석 포함)에 넣지 않는다 — `test/fixtures/timeline-slice.json`은 좌표를 옮긴 가짜이고, **옮긴 양도 적지 않는다** (적으면 되돌릴 수 있다).

## 파일 크기와 책임 — 운용 우선

이 프로젝트는 빨리 만드는 것보다 **나중에 고치기 쉬운 것**을 우선한다. v1에서 고치기 어려웠던 파일은
`SightingForm.tsx`(1,483줄)·`JournalComposer.tsx`(809줄)·`server/index.js`(723줄)였고,
33~357줄이던 `src/lib/*`는 문제가 없었다. 아래 숫자는 그 경험에서 나온 기준이다.

줄 수는 **빈 줄과 주석을 빼고** 센다 (주석을 많이 달수록 불리해지면 안 된다).

| 대상 | 목표 | 상한 |
|---|---|---|
| 컴포넌트 파일 (`.tsx`) | 150줄 | 300줄 |
| 로직 모듈 (`.ts`) | 200줄 | 400줄 |
| 함수 하나 | 40줄 | 80줄 |

- **상한에 닿으면 기능을 더하기 전에 먼저 쪼갠다.** 더한 뒤에 쪼개려면 이미 늦다.
- 줄 수는 신호일 뿐이고 진짜 기준은 **한 파일 = 한 가지 책임**이다. 파일이 하는 일을 한 문장으로
  말했을 때 "그리고"가 들어가면 쪼갤 자리다.
- **숫자를 맞추려고 기계적으로 자르지 않는다.** 서로를 계속 불러야 하는 100줄짜리 열 개는
  600줄짜리 하나보다 고치기 어렵다. 같이 바뀌는 것은 같이 둔다.
- 컴포넌트에는 화면만 둔다. 계산·변환·저장은 `lib`의 순수 함수로 빼서 테스트를 붙인다.
  의존 방향은 화면 → 상태(훅) → 도메인 로직 → 저장소 한쪽으로만 흐른다.
- 예외: 테스트 파일, 타입 정의, 상수·데이터 표, 생성된 코드. 상한을 넘겨야 할 이유가 있으면
  파일 맨 위 주석에 그 이유를 적는다.

## 겉모습은 토큰으로만 바꾼다

설정에서 화면 테마를 바꿀 수 있게 한다 (후보 6종, `DESIGN_PROMPT.md` 참고). 테마가 늘어도 고칠 곳이
늘지 않게 하려면 아래를 지켜야 한다. Figma 시안(`ref_design/`)은 컴포넌트 안에서 `isVintage`·`isGlass`
같은 분기를 33군데 썼다 — **이 방식을 가져오지 않는다.**

- **컴포넌트는 테마의 이름을 모른다.** 컴포넌트 안에서 테마 id를 검사하지 않는다. 색·모서리·그림자·
  테두리·글꼴은 전부 토큰(CSS 변수)으로 받는다.
- **배치와 문구는 모든 테마에서 같다.** 테마마다 다른 컴포넌트나 다른 문구를 만들지 않는다.
- 토큰으로 표현되지 않는 차이(배경 장식 등)가 필요하면 **테마 정의 쪽에** 둔다. 새 토큰을 더하는 것은
  되지만, 특정 테마만을 위한 분기를 컴포넌트에 넣는 것은 안 된다.
- 테마를 더하거나 고칠 때 모든 테마에서 본문 대비 4.5:1 이상을 확인한다.
- **새 카드의 디자인은 앱 테마와 무관하게 하나다.** 공유된 카드는 어느 사용자 것이든 같아 보여야 한다. 기준은 사용자의 Card Reveal 디자인(`ref_design/Card Reveal.html`)이고, 값은 `features/dex/cardLook.ts`(공통)·`cardStyle.ts`(기록마다의 색) 두 곳에 있다 — 화면의 카드와 내보내는 카드가 함께 쓴다. **카드에 등급을 매기지 않는다** (2026-09-22 결정). `Sighting.tier`는 옛 백업 호환으로만 남아 있고 새 기록은 1이다 — 등급·별·희귀도 표시를 되살리지 말 것.

## 코드 주석 (필수)

- **함수를 만들면 위에 JSDoc 블록을 반드시 단다.** 무엇을 하는지 + 실패/경계 동작을 적는다.
  ```ts
  /**
   * 이미지 File에서 GPS + 촬영 시각을 파싱한다.
   * EXIF가 없거나 파싱이 실패하면 {} 를 반환 (에러 전파하지 않음).
   */
  export async function parseExif(file: File): Promise<ExifInfo> {
  ```
- **타입·인터페이스 필드에 의미가 안 드러나면 `/** ... */`를 붙인다.**
  ```ts
  /** ISO 문자열 (DateTimeOriginal 또는 CreateDate) */
  capturedAt?: string
  ```
- **"왜"가 자명하지 않은 줄에는 이유를 적는다.** 특히 우회·제약·함정.
- 주석은 **한국어**로 쓴다.
- 코드가 이미 말해주는 것을 중복해 적지 않는다. 값은 **의도·제약·실패 동작**에 있다.
- v1에서 복사해 오는 파일은 주석을 그대로 가져온다. 주석에 실측으로 알아낸 함정이 적혀 있다.
- **`node --test`가 직접 읽는 파일(테스트가 import하는 순수 로직)에서 값을 import할 때는 확장자 `.ts`를 적는다** (`from './cardLook.ts'`). node는 확장자 없는 경로를 못 푼다. Vite·tsc는 둘 다 받아들인다 (`allowImportingTsExtensions`). 그 줄 위에 이유를 한 줄 적는다.

## 꾸미기(관찰지) 확정 결정 — v1에서 이어받음, 다시 묻지 말 것

- **스티커는 일단 제거** (2026-09-04). 글자만 얹는 텍스트 버튼이라 이름이 가리키는 그래픽이 없었다. **최종 결정은 아니다** — 나중에 SVG로 다시 만들지 미정이다. 그래서 **초안의 `stickers` 배열은 지우지 않고 보존**하고, 스티커가 든 초안을 열면 사용자에게 알린다.
- **셔터는 초 단위 실수로 저장한다.** `1/200` 표기는 표시·입력 형식일 뿐이다. 옮겨 온 `src/lib/format.ts`의 `formatExposure`는 v1에서 이미 고쳐진 판이다 (`0.7초` → `1/1.43s`, 왕복 오차 1% 이내, 테스트 있음). 역수를 정수로 반올림하던 옛 방식으로 되돌리지 말 것.
- **이름 테두리 기본은 한 겹** (2026-09-07). 두 겹은 `TitleStyle.double` 스위치로 남겼다. 되돌리지 말 것.
  (1) **프리셋은 `double`을 건드리지 않는다** — 색을 고르다가 켜 둔 겹 수가 꺼지면 안 된다.
  (2) `titleStyle`은 있는데 `double` 키가 없는 초안은 **두 겹으로 읽는다** — 두 겹만 있던 시절에 저장된 것이라, 기본값을 씌우면 이미 만든 관찰지 모양이 말없이 바뀐다.
- **학명(`latin`)은 삭제하지 않는다.** 종 자동 판별의 자동 입력 착지점이다. 표시를 줄이고 토글 대상으로만 둔다.
- **초안 스키마는 가산 확장만.** 키를 바꾸거나 형태를 파괴적으로 고치지 않는다. 새 정보는 형제 키로 더한다. v1에서 만든 초안이 v2에서 열려야 한다.
